import { Item } from "../items/Item";

export class RewardController {

    getConcreteReward(reward: Item) {
        if (reward) {
            switch(reward.type) {

            }
        }
        return undefined;
    }

}